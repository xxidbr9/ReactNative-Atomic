import { RFPercentage as rPer } from "react-native-responsive-fontsize";

/* 
 ? it is tested on iphone 8 plus 
 */

export default {
    h1: rPer(3.1), // 23
    h2: rPer(2.8), // 20
    h3: rPer(2.4), // 18
    h4: rPer(2.2), // 16
    h5: rPer(1.9), // 14
    h6: rPer(1.6) // 12
};
